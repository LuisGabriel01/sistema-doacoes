[project]
name = "sistema-doacoes"
version = "0.0.1"
dependencies = [
  "flask",
  "flask_security",
  "sqlalchemy",
  "sqlalchemy_utils",
  "paracelsus",
  "argon2-cffi",
]

[tool.setuptools]
packages = ['app']

[tool.paracelsus]
base = "app.database:db"
imports = ["app.models"]
